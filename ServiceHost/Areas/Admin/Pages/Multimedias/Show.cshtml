@model List<Haidarieh.Application.Contracts.Multimedia.MultimediaViewModel>
@{ }
<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h4 class="modal-title">سوابق مراسم</h4>
</div>

<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default">
            @*<div class="panel-heading">
                    <h3 class="panel-title">@Model فایلهای</h3>
                </div>*@
            <div class="panel-body">
                <div class="row">
                    <div class="col-md-4 col-sm-4 col-xs-4">
                        <div class="table table-striped table-bordered">
                            @foreach (var item in Model)
                            {
                <div>
                    <img src="~/@item.FileAddress" style="width: 100px; height: 100px" />
                </div>
                                <div>
                                    @*<form class="btn btn-danger" asp-page="Index" method="post" asp-route-id="@item.Id" asp-page-handler="Delete">
                                            <i class="fa fa-edit"></i>حذف
                                        </form>*@

                                    @*<form method="post" asp-page="Index" asp-route-id="@item.Id" asp-page-handler="Delete" class="d-inline">
                                        <button type="submit" class="btn btn-danger text-white"> Delete</button>
                                    </form>*@

                                    @*<a class="btn btn-danger pull-right m-rl-5"
                                           href="#showmodal=@Url.Page("./Index","show", new { id = item.Id })">
                                           <i class="fa fa-edit"></i>حذف1
                                    </a>*@
                                </div>}
                        </div>
                    </div>
                </div>
            </div>
        </div>
        @*</div>
            </div>*@
        <div class="modal-footer">
            <button type="button" class="btn btn-default waves-effect" data-dismiss="modal">بستن</button>
        </div>

        @*<table id="datatable" class="table table-striped table-bordered">
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                <img src="~/@item.FileAddress" style="width: 70px; height: 70px" />
                            </td>
                        </tr>
                    }
                </tbody>
            </table>*@
    </div>
</div>

<script src="~/adminTheme/assets/datatables/jquery.dataTables.min.js"></script>
<script src="~/adminTheme/assets/datatables/dataTables.bootstrap.js"></script>

<script type="text/javascript">
    $(document).ready(function () {
        $('#datatable').dataTable();
    });
</script>


